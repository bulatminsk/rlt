version: 2

sources:
    - name: cls_realt
      description: "cleansing level"
      tables:
          - name: aps
            columns:
              - name: address
                description: "source date"
                tests:
                    -  unique
                    -  not_null
